{
  "name": "sdk-e-auth",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @sdk-e/db generate",
    "db:migrate": "pnpm --filter @sdk-e/db migrate",
    "db:studio": "pnpm --filter @sdk-e/db studio"
  },
  "devDependencies": {
    "turbo": "^2.10.11",
    "typescript": "^5.9.2"
  }
}
